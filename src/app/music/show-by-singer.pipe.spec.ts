import { ShowBySingerPipe } from './show-by-singer.pipe';

describe('ShowBySingerPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowBySingerPipe();
    expect(pipe).toBeTruthy();
  });
});
